[metadata]
name = tabula-java

[options]
package_dir =
    = src
packages = find:
include_package_data = True

[options.packages.find]
where = src

[options.package_data]
tabula_java_runtime = runtime/**/*

[bdist_wheel]
root_is_pure = False
